/*

Array :- They are ordered DS.There is intrinsic ordering of data.

Array Operations and their BIG O notation :- 
    - Access :- O(1)
    - Insertion :- O(1) or O(N)         // if inserted at last then O(1) otherwise if inserted at start then O(N)
    - Removal :- O(1) or O(N)         // if removed at last then O(1) otherwise if inserted at start then O(N)
    - Searching :-

Object Methods :-
    - push :- O(1)
    - pop :- O(1)
    - shift :- O(N)
    - unshift :- O(N)
    - concat :- O(N)
    - slice :- O(N)
    - splice :- O(N)
    - sort :- O(N*LogN)
    - forEach :- O(N)
    - map :- O(N)
    - filter :- O(N)
    - reduce :- O(N)

*/

