/*

It is build using webpack.And that bundle is put into webpack dev server and that url is shown to us. 
First of all index.html is loaded. There we have an element with id root.
React CLI inserts the scripts i.e bundles.
Webpack tries to bundle all the files starting from index.js


How React builds?
    - It uses babel to transpile JSX and webpack to bundle the application.


*/